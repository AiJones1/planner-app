import { useEffect, useState } from 'react';
import { api } from '@/lib/api';

interface User {
    user_id: string;
    name: string;
    email: string;

}

export default function LoginPage() {
    
}